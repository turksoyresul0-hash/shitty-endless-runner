(()=>{'use strict';const KEY='fpsWallet',MONEY='money';function read(){try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return {}}}function save(w){localStorage.setItem(KEY,JSON.stringify(w));return w}function wallet(){const w=read();if(!Number.isFinite(Number(w.money))){const legacy=Math.max(0,Number(localStorage.getItem(MONEY))||0);w.money=legacy;save(w)}w.money=Math.max(0,Number(w.money)||0);return w}function reward(d){d=d||{};const win=d.winner&&d.winner!=='draw'&&window.__fpsLocalTeam===d.winner;kills=Math.max(0,Number(d.kills)||0);assists=Math.max(0,Number(d.assists)||0);const base=win?650:d.winner==='draw'?350:250;const money=base+kills*50+assists*25;const w=wallet();w.money+=money;w.lastMatchReward={money,win,kills,assists,at:Date.now()};save(w);const total=w.money;localStorage.setItem(MONEY,String(total));for(const id of ['money','lbMoney','smoney']){const e=document.getElementById(id);if(e)e.textContent=String(total)}window.dispatchEvent(new CustomEvent('fps:money',{detail:{money:total,delta:money}}));window.dispatchEvent(new CustomEvent('fps:wallet-reward',{detail:{money,win,kills,assists,total,gameMoney:total}}));return{money,win,total,gameMoney:total}}window.addEventListener('fps:match-result',e=>{const d=e.detail||{};if(d.__rewardApplied)return;d.__rewardApplied=true;reward(d)});window.fpsMatchRewards={reward,getWallet:wallet};})();